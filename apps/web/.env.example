# Kopieer naar .env.local voor lokale ontwikkeling (geen secrets committen).

NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon-key>

# Externe boeking (consultatie-CTA op home). Leeg laten = link naar /contact
NEXT_PUBLIC_BOOKING_URL=
